# ============================================================
# SafeSend Worker (v1.5.9) — Environment Variable Template
# ============================================================
# Copy this file to `.env` (for local testing) or
# set these values under:
# Cloudflare Dashboard → Workers → Settings → Variables
# ============================================================

# === Plaintext Address Lists ================================
# Each list accepts hex addresses (0x...) separated by newlines,
# commas, or spaces. All are case-insensitive.

# OFAC sanctions list — high priority (auto-block)
OFACLIST="0x72a5843cc08275c8171e582972aa4fda8c397b2a
0x1234567890abcdef1234567890abcdef12345678"

# Optional supplemental sanctions or watchlist
OFAC_SET=""

# Internal bad list — addresses RiskXLabs marks as compromised or scam
BADLIST="0xabcdefabcdefabcdefabcdefabcdefabcdefabcd"

# Known bad ENS names (optional; not yet used in v1.5.9)
BAD_ENS="scamwallet.eth
fraudfund.eth"

# === External API Keys (optional for future enrichment) =====
# These are not required for v1.5.9 but can be added later.
# Example: Cloudflare Secrets -> ETHERSCAN_API_KEY, COINGECKO_API_KEY

ETHERSCAN_API_KEY=""
COINGECKO_API_KEY=""

# === Metadata ===============================================
# Optional identification tag shown in responses
SAFE_SEND_ORG="RiskXLabs"
